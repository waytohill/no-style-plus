---
layout: page
title: "404 — Page Not Found"
permalink: /404.html
sitemap: false
---

The page you're looking for doesn't exist. It may have been moved, renamed, or deleted.

Try the [home page](/), [archive](/archive/), or [search](/search/) (press `/` from any page).

## Recent posts

<ul class="archive-list">
{%- for post in site.posts limit: 5 -%}
  {%- assign desc = post.description | default: "" | strip -%}
  {%- if desc == "" -%}
    {%- assign desc = post.excerpt | strip_html | normalize_whitespace | truncate: 90 -%}
  {%- endif -%}
  <li>
    <span>{{ post.date | date: site.theme_config.date_format }}</span>
    <a href="{{ post.url | relative_url }}">{{ post.title }}</a>
    {%- if desc != "" -%}
      <div class="post-list-desc">{{ desc }}</div>
    {%- endif -%}
  </li>
{%- endfor -%}
</ul>
